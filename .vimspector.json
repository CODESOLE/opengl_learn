{
  "configurations": {
    "Launch MSVC Target": {
      "adapter": "CodeLLDB",
      "configuration": {
        "type": "rust",
        "request": "launch",
        "program": "${workspaceRoot}/build/debug/opengl_learn",
        "cwd": "${workspaceRoot}",
        "env": {
          "BASEPATH": "${workspaceRoot}"
        },
        "args": []
      }
    }
  }
}
